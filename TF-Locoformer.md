
## Terms

### FFT窓長

- STFTで波形を切り出す一区間の長さ
- 1フレームがどれだけ過去を見ているかを決める。窓が長いほど残響の尾を1フレーム内に収められるので残響環境に弱い

### 線形層

- 各位置を独立に処理し、チャネル方向だけを混ぜる
- 隣の時刻・周波数の情報は一切見ないので、局所構造を捉えられない

### Conv1D

- カーネル幅Kの窓を系列方向にスライドさせ、隣接するK個の位置をまとめて処理する→局所情報が入る
- K=1にすると隣を見なくなり、線形層と完全に等価

## Abstract

- TF領域のデュアルパスモデルは、高忠実度な音声分離を達成している
- 従来のいくつかのSoTAモデルはRNNに依存しているが、この依存によりT**ransformerブロックが持つ並列化可能性・スケーラビリティ・汎用性が失われている**
- 本研究ではSoTA性能を維持しつつ**TF領域デュアルパスモデルからRNNを取り除く**ことに焦点を当てる
- 本モデルは、線形層の代わりに畳み込み層を用いたFFNによって局所情報を捉え、自己注意機構には大域的パターンの捕捉に専念させる
- このFFNを自己注意の前後に2つ配置することで、局所モデリング能力を強化する
- さらに、TF領域デュアルパスモデル向けの新しい正規化層も導入する
- 分離および強調のデータセットを用いた実験により、提案モデルがRNNフリーのアーキテクチャで複数のベンチマークにおいてSoTAと同等または上回る性能を達成することが示された

## 1. Introduction

### 背景

- 初期は deep clustering など、TF振幅領域でマスクを推定するアプローチ
- TasNet系が学習可能なエンコーダ/デコーダで性能を大きく改善
- 局所・大域モデリングを交互に行うデュアルパスが時間領域E2Eの主流に
- 近年は時間軸と周波数軸を交互に処理するTF領域デュアルパスが好成績
- FFT窓長は学習可能エンコーダのカーネルより長いため、TF領域は残響環境に強い可能性

### 問題意識

- RNNは推論時のメモリ効率は良いが、並列化できず学習が遅い
- Transformerは並列学習・プロンプト対応・スケーラビリティを持つが、TF領域デュアルパスのSoTAは依然RNNベース
- → 本論文の第一歩は「同規模のRNNフリーモデルでSoTA相当の性能が出るか」の検証

### 技術的仮説

- 音声処理には大域・局所の両方が必要だが、Transformerブロックは局所モデリング能力を本質的に欠く
- 時間領域デュアルパスでは intra-chunk の自己注意が構造上局所に限定されるため機能したが、TF領域では局所性が強制されず不利
- 局所情報を捉えられるRNNが強いのはそのため
- → RNNの代わりに畳み込みをTransformer内に入れる

### 提案

- FFNの2つの線形層を Conv1D / Deconv1D に置換
- SwiGLU活性化を採用し、macaron型に倣ってFFNを自己注意の前後に配置
- TF領域向けの新しい正規化層を追加
- これらが局所モデリング能力を高め、RNNベースSoTAと同等以上になることを実験で示す
  

## 2. TF-Locoformer

### 2.1. TF-Locoformerの概要

![[Pasted image 20260723103535.png]]

### 入出力

- 入力：N人の音声＋雑音が混ざった単チャネル波形 x（長さL）
- 出力：分離されたN音源の波形。マスク推定ではなく**複素スペクトルマッピング**

**処理の流れ**

1. **STFT** → X ∈ R^(2×T×F)（2＝実部/虚部、T＝フレーム数、F＝周波数ビン数）
2. **エンコード**：Conv2D + gLN で X → Z ∈ R^(D×T×F)（Dが特徴次元）… 式(1)
3. **Locoformerブロック ×B回**（各ブロック内で周波数→時間の順に処理）
4. **デコード**：DeConv2D で Z → Ŝ ∈ R^(2×N×T×F) … 式(5)
5. **逆STFT** → 時間領域の分離波形

**ブロック内部（式2〜4）** すべて残差接続で、macaron型の3段構成：

| 順   | 処理                | 役割                                |
| --- | ----------------- | --------------------------------- |
| ①   | +ConvSwiGLU(Z)/2  | 局所モデリング（1/2倍はmacaron型の流儀）         |
| ②   |  MHSA(Norm(Z))    | 大域モデリング（Hヘッド、各D/H次元、RoPEで相対位置を付与） |
| ③   | + ConvSwiGLU(Z)/2 | 局所モデリング                           |

### 周波数モデリングと時間モデリングの違い

- 中身の構造は**完全に同一**、違うのは「どの軸を系列長として扱うか」だけ

#### 周波数モデリング

- Z を T×D×F に置換 → **F が系列長**（各フレーム内で周波数方向に注意を張る）

#### 時間モデリング

- Z を F×D×T に置換 → **T が系列長**（各周波数ビンで時間方向に注意を張る）

|記号|役割|
|---|---|
|D|チャネル（特徴次元）|
|F または T|系列長（注意を張る軸）|
|もう一方|バッチに畳み込む（処理しない）|

### 2.2. ConvSwiGLUモジュール

#### 動機

- RNNがTF領域デュアルパスで強いのは、隠れ状態の逐次更新が**局所情報を自然に捉える**から
- 一方MHSAは、注意重みを常に局所的に滑らかにするのが難しい
- → 局所モデリングを明示的に組み込んだFFNを作る

#### 提案：ConvSwiGLU

- 従来のFFN（線形層×2）に対し、2つの変更を加えたもの

| 変更点     | 内容                                          |
| ------- | ------------------------------------------- |
| 畳み込み化   | 線形層 → Conv1D / Deconv1D<br>隣接位置を見るので局所情報が入る |
| SwiGLU化 | NLPでSwishより高性能とされるゲート付き活性化を採用               |

#### 処理

```
Z ← Norm(Z)                             (6) 正規化
Z ← Swish(Conv1D(Z)) ⊗ Conv1D(Z)        (7) 2分岐 → ゲーティング（⊗は要素積）
Z ← Deconv1D(Z)                         (8) 元の次元へ戻す
```

式(7)は Conv1D を**2本並列**に通し、片方に Swish をかけてもう片方の「ゲート」として掛け合わせる形です。

#### 配置

- 自己注意の前後**両方**に置く（macaron型）→ 局所モデリングをさらに強化

要するに「**RNNの役割を畳み込みで代替し、それをFFNの中に埋め込んで注意の前後に挟む**」のが本節の主張です。

### 2.3. TF領域デュアルパスモデルのためのグループ正規化

- 一般に、TF領域デュアルパスモデルはNorm層としてレイヤ正規化または二乗平均平方根正規化（RMSNorm）[23] を利用し、各TFビンのD次元ベクトルを正規化する。
    
- しかし、目的が複数話者の分離であることを踏まえると、各D次元ベクトル Z_{t,f} を話者IDのような分離された概念に対応するグループに分割するよう促すことが有益ではないかと我々は考える。
    
- RMSGroupNormでは、各D次元ベクトル Z_{t,f} を D/G 次元のベクトルが G 個積み重なったものとみなし（G はグループ数）、各 D/G 次元ベクトルを個別に正規化する。
    
- これによりモデルは各D次元ベクトルをいくつかのグループに分離するよう促され、音声分離に役立つ可能性がある。
    
- 画像処理におけるグループ正規化 [24] とは異なり、我々は各TFビンごとに正規化する点に注意されたい。
    
- RMSNormと同様に、RMSGroupNormは2つのD次元学習可能パラメータによるアフィン変換を備えている。
    
- なお、G = 1 は元のRMSNormに対応する。
    
- 実験では、RMSGroupNormが本モデルにおいてRMSNormよりもわずかながら一貫して良い性能を与えることを示す。
    

#### 表1: ハイパーパラメータ記号と3つのモデルサイズ（Small/Medium/Large）のデフォルト設定

|記号|説明|S|M|L|
|---|---|---|---|---|
|D|各TFビンの埋め込み次元|96|128|128|
|B|Locoformerブロック数|4|6|9|
|C|Conv-SwiGLU内の隠れ次元|256|384|384|
|K|Conv1D・Deconv1Dのカーネルサイズ|4|4|4|
|S|Conv1D・Deconv1Dのストライド|1|1|1|
|H|自己注意のヘッド数|4|4|4|
|G|RMSGroupNormのグループ数|4|4|4|
|-|パラメータ数 [M]|5.0|15.0|22.5|

---

## 3. Experiments（実験）

### 3.1. データセットと実験設定

- モデルの評価には、3つの音声分離コーパス WSJ0-2mix [1]、Libri2mix [25]、WHAMR! [26]、および音声強調コーパスであるInterspeech DNS-challenge 2020データセット（DNSと表記）[27] を用いた。
    
- 音声分離データセットでは常にサンプリング周波数8 kHzの完全重畳minバージョンを使用し、DNSデータセットのサンプリング周波数は16 kHzとした。
    
- **WSJ0-2mix** は、WSJ0コーパスの発話からなる2話者混合を含む。学習・検証・テストセットの総時間長はそれぞれ30時間、10時間、5時間である。
    
- **Libri2Mix** は、LibriSpeech [28] の発話からなる2話者混合を含む。学習・検証・テストセットの総時間長はそれぞれ212時間、11時間、11時間である。
    
- **WHAMR!** は、WSJ0-2mixの雑音・残響版である。モデルは残響除去・雑音抑圧・分離を同時に実行するよう学習される。
    
- **DNS** は2700時間の学習データと300時間の検証データを持ち、公式スクリプト [27] を用いてシミュレートされている。テストには非ブラインド無響テストセットを使用する。
    
- すべての実験はESPnet-SEパイプライン [29] を用いて実施した。
    
- ハイパーパラメータの概要とデフォルトのモデル設定を表1に示す。
    
- 主にMediumモデルを検証する。一部のデータセットでは、先行研究とのより公平な比較のためSmallモデルとLargeモデルも評価する。
    
- 強い残響を持つWHAMR!においてのみ、TF-GridNetに倣って K = 8 とし C を半分にした。
    
- STFTの窓長とホップ長は全データセットで16 msと8 msに設定したが、WHAMR!のみ窓長を32 msとした。
    
- 重み減衰1e-2のAdamWオプティマイザ [30] を使用する。
    
- 学習率はまず最初の4000ステップで0から1e-3まで線形に増加させる。
    
- その後、検証損失が3エポック改善しない場合に学習率を0.5倍に減衰させる。
    
- 学習は最大150エポックまで行い、検証損失が10エポック改善しない場合は早期終了する（注: DNSデータセットでは学習データが非常に大きいため、21,000学習ステップを1エポックと定義する）。
    
- 動的ミキシング（DM）を用いる場合は最大200エポックまで学習し、学習率減衰と早期終了はSmall・Mediumモデルでは75エポック以降、Largeモデルでは65エポック以降に適用する。
    
- バッチサイズは4、入力サンプル長は4秒である。
    
- 勾配のL2ノルムは5でクリッピングする。
    
- 各入力混合信号はその標準偏差で除算して正規化する。
    
- 音声分離の損失関数には、置換不変 [1, 31] なスケール不変SN比（SI-SNR）[32] を用いる。
    
- 音声強調には、時間領域L1損失にTF領域マルチ解像度L1損失 [33] を加えたものを用いた。
    
- 4つのFFT窓長 {256, 512, 768, 1024} を50%オーバーラップで使用した。
    
- 評価では、検証セットで最良の損失を示した5つのモデルチェックポイントの重み平均を用いる。
    
- 評価指標としては、SI-SNR改善量（SI-SNRi）、信号対歪み比改善量（SDRi）[34]、短時間客観的了解度（STOI）[35]、広帯域知覚音声品質評価（PESQ-WB）[36] を使用する。
    

### 3.2. 無響環境での音声分離と音声強調

- 表2は、WSJ0-2mixデータセットにおける提案モデルの性能と文献で報告された結果を比較したものである。
    
- まず、いずれもRNNを持つTF領域モデルであるTFPSNetとTF-GridNetに注目する。
    
- これらはすべて B = 6 のデュアルパスブロックを持つため、Mediumモデルと比較する。
    
- 結果は、TF-LocoformerがRNNベースのTF領域モデルと同等以上の性能を達成することを示しており、強い局所モデリングを導入すればRNNフリーのモデルもTF領域で良好に機能しうることを示唆している。
    
- 次に、TF-Locoformerを他のモデルと比較する。
    
- WSJ0-2mixにおける現行SoTAモデルであるSepTDAは9個のマルチパスブロックを持つため、この比較にはLargeモデルが適している。
    
- TF-Locoformerはここでも、従来のSoTAモデルと同等以上の性能を達成している。
    
- また、動的ミキシングを用いてモデルを学習させたところ、顕著な改善が観察された。
    
- WSJ0-2mixにおける性能は飽和していると論じられることが多いが、我々はSI-SNRiが低いサンプルにはまだ改善の余地があることを見出した。
    
- 図2は、WSJ0-2mixテストセットにおける提案モデルのSI-SNRiの箱ひげ図を示している。
    
- より大きなモデルほど失敗の数が減り、より頑健に動作することが観察できる。
    
- 動的ミキシングもデータ拡張によって同様の効果をもたらす。
    
- より大規模な音声分離・強調データセットにおいても、TF-Locoformerは良好に機能する。
    
- 表3と表4は、それぞれLibri2MixデータセットとDNSデータセットにおけるTF-Locoformerと競合モデルの性能を示している。
    
- 提案するTF-Locoformerは両データセットで最良の性能を与えており、その潜在的なスケーラビリティを実証している。
    
- 加えて、TF-Locoformerは雑音抑圧にも有効である。
    

#### 表2: WSJ0-2mixにおける先行モデルとの比較（*は動的ミキシング時に速度摂動を使用。「-」は原論文で未報告。単位[dB]）

|System|Domain|#params[M]|SI-SNRi (w/o DM)|SDRi (w/o DM)|SI-SNRi (w/ DM)|SDRi (w/ DM)|
|---|---|---|---|---|---|---|
|DPRNN [3]|T|2.6|18.8|19.0|-|-|
|DPTNet [4]|T|2.7|20.2|20.6|-|-|
|Wavesplit [37]|T|29.0|21.0|21.2|22.2|22.3|
|SepFormer* [5]|T|25.7|20.4|20.5|22.3|22.4|
|TFPSNet [6]|TF|2.7|21.1|21.3|-|-|
|QDPN* [10]|T|200.0|22.1|-|23.6|-|
|TF-GridNet [7]|TF|14.4|23.5|23.6|-|-|
|MossFormer2* [11]|T|55.7|-|-|24.1|-|
|SepTDA [38]|T|21.2|24.0|23.9|-|-|
|**TF-Locoformer (S)**|TF|5.0|22.0|22.1|22.8|23.0|
|**TF-Locoformer (M)**|TF|15.0|23.6|23.8|24.6|24.7|
|**TF-Locoformer (L)**|TF|22.5|**24.2**|**24.3**|**25.1**|**25.2**|

#### 表3: Libri2Mixにおける先行モデルとの比較（DM・速度摂動は不使用。単位[dB]）

|System|Domain|#params[M]|SI-SNRi|SDRi|
|---|---|---|---|---|
|Conv-TasNet [2]|T|5.1|14.7|-|
|Wavesplit [37]|T|29.0|19.5|20.0|
|SepFormer [5]|T|25.7|19.2|19.4|
|MossFormer2 [11]|T|55.7|21.7|-|
|**TF-Locoformer (M)**|TF|15.0|**22.1**|**22.2**|

#### 表4: DNS2020非ブラインドテストセットにおける非因果モデルとの比較（SI-SNRは[dB]）

|System|#params[M]|SI-SNR|STOI|PESQ-WB|
|---|---|---|---|---|
|Noisy|-|9.1|91.5|1.58|
|MFNet [39]|6.1|20.3|98.0|3.43|
|USES [15]|3.1|21.2|98.1|3.46|
|**TF-Locoformer (M)**|15.0|**23.3**|**98.8**|**3.72**|

#### 図2の数値（WSJ0-2mixテストセットのSI-SNRi平均±標準偏差）

- Small: 22.0 ± 3.7 / Small+DM: 22.8 ± 3.3
- Medium: 23.6 ± 3.2 / Medium+DM: 24.6 ± 2.7
- Large: 24.2 ± 2.9 / Large+DM: 25.1 ± 2.6

### 3.3. 雑音・残響環境での音声分離

- 表5は、WHAMR!データセットにおける提案モデルと先行モデルの性能を比較したものである。
    
- 現行SoTAモデルであるTF-GridNetと公平に比較するため、Smallモデルを評価した。これはTF-GridNetがWHAMR!の評価において B = 4 のデュアルパスブロックで構成されていたためである [7]。
    
- 結果は、提案モデルがここでもSoTAモデルを上回ることを示している。
    
- Mediumモデルはさらに高いスコアを示しており、提案モデルがより大きな構成を用いることでさらに良い性能を達成しうることを示唆している。
    
- この比較はTF-Locoformerが有効であることを示すだけでなく、現在の最良のTF領域モデルが残響条件下で最良の時間領域手法よりも優れた性能を発揮することも実証している。
    
- QDPN [10] やMossFormer2 [11] のような一部のモデルはWSJ0-2mixではSmall TF-Locoformerと同等以上の性能を示したが、WHAMR!では後者が動的ミキシングなしでもより良い性能を達成している。
    
- この観察は、時間領域モデルがエンコーダ・デコーダの短いカーネルサイズ（例えば2 ms）のために残響に苦戦すると主張する [8] と一致している。
    
- 結果は、より現実世界の応用を代表する残響条件下でのTF領域モデルの優位性を明確に示している。
    

#### 表5: WHAMR!における先行モデルとの比較（DM*は速度摂動付きDMを示す。単位[dB]）

|System|Domain|#params[M]|SI-SNRi|SDRi|
|---|---|---|---|---|
|Wavesplit+DM [37]|T|29.0|13.2|12.2|
|SepFormer+DM* [5]|T|25.7|14.0|13.0|
|QDPN+DM* [10]|T|200.0|14.4|-|
|MossFormer2+DM* [11]|T|55.7|17.0|-|
|TF-GridNet [7]|TF|5.5|17.1|15.6|
|**TF-Locoformer (S)**|TF|5.0|17.4|15.9|
|**TF-Locoformer (M)**|TF|15.0|**18.5**|**16.9**|

### 3.4. アブレーション研究

- 本モデルの各モジュール（畳み込み層、ConvSwiGLU FFN、Macaron型アーキテクチャ、RMSGroupNorm）の有効性を検証するため、アブレーション研究を行った。
    
- 図3は、WSJ0-2mixテストセットにおける、異なるカーネルサイズのMediumモデルの平均SI-SNRiを示している。
    
- デフォルトのモデル設定が K = 4、C = 384 であったため、すべてのモデルのサイズをおおよそ揃えるために C = 1536/K と設定した。
    
- RMSGroupNormのグループ化が結果に及ぼす潜在的な影響を避けるため、ここでは正規化層としてRMSNormを用いた。
    
- K = 1 は通常の線形層と等価であり、その場合各ブロックは純粋なTransformerブロックとほぼ同じになる。
    
- 結果は畳み込み層の重要性を明確に示しており、K ≥ 2 のモデルは K = 1 のモデルよりもはるかに良い性能を示す。
    
- 同時に、カーネルが長いモデルはSwish活性化とそれに続くゲーティングへの入力の隠れ次元 C が小さくなるため、計算効率が高い。
    
- しかし、効率と性能の間にはトレードオフが観察され、カーネルが長すぎるモデル（例えば K = 8）は最良の性能を与えない。
    
- むしろ、K = 3 と K = 4 が最良の結果につながることが分かった。
    
- 表6では、モデルサイズを一定に保ちつつ、元のTransformerブロックに対するその他の変更の寄与を評価する。
    
- A1は1つ目のConvSwiGLUモジュールを除去し、2つ目のConvSwiGLUの隠れ次元を 2C に増加させたものである。
    
- A2はさらにSwiGLU活性化をSwishに置き換え（すなわち図1の右側のConv1D分岐を除去し）、隠れ次元を 3C に増加させたものである。
    
- 結果は、いずれの設計も分離性能の向上に寄与することを示している。
    
- 最後に、表7でRMSNormとRMSGroupNormを用いたモデルの性能を比較する。
    
- 改善はわずかであるものの、提案するRMSGroupNormは一貫してより良い性能につながる。
    
- この結果は、2.3節で議論したように、各TFビン内でグループを形成するようモデルを促すことがTF領域デュアルパス分離モデルにとって有効でありうることを示している。
    

#### 表6: WSJ0-2mixにおけるアブレーション研究（単位[dB]）

||System|SI-SDRi|SDRi|
|---|---|---|---|
|A0|TF-Locoformer (M)|**23.6**|**23.8**|
|A1|Macaron型 → 単一ConvSwiGLU|22.8|22.9|
|A2|SwiGLU → Swish活性化|22.2|22.4|

#### 表7: WSJ0-2mixにおける正規化層の比較（単位[dB]）

|System|RMSNorm SI-SNRi|RMSNorm SDRi|RMSGroupNorm SI-SNRi|RMSGroupNorm SDRi|
|---|---|---|---|---|
|TF-Locoformer (S)|21.7|21.9|**22.0**|**22.1**|
|TF-Locoformer (M)|23.5|23.6|**23.6**|**23.8**|
|TF-Locoformer (L)|24.0|24.1|**24.2**|**24.3**|

---

## 4. Conclusion（結論）

- 我々は、TF領域デュアルパスモデリングにおいて大域モデリングと局所モデリングを効果的に統合する音声分離モデル、TF-Locoformerを提案した。
- 従来のSoTAなTF領域デュアルパスモデルがRNNに基づいているのに対し、我々は並列化可能なアーキテクチャ、潜在的なスケーラビリティ、汎用性（例えばタスク指定のためのプロンプティング）といった利点を考慮し、Transformerブロックに基づくモデルを開発した。
- 局所モデリングと大域モデリングの両方を行うRNNの成功に着想を得て、自己注意が大域モデリングを、畳み込みが局所モデリングを担うモデルを設計した。
- また、TF領域モデルのための効果的な正規化層も提案した。
- 実験的比較により、提案モデルが4つの広く用いられるベンチマークにおいて従来のSoTAモデルと同等以上の性能を与えることが実証された。
- アブレーション研究を通じて、局所モデリングの重要性を示した。
- 最後に、実験はTF領域モデルが時間領域モデルよりも残響をはるかにうまく扱えることを示唆した。
- 今後は、TF-Locoformerのスケーラビリティ、および音楽や一般音響の分離における有効性を検証していく予定である。

---

## 5. References（参考文献）

※ 参考文献リストは原文のまま（[1]–[39]）。原著PDFを参照のこと。主要なものは以下の通り:

- [2] Conv-TasNet (Luo & Mesgarani, 2019)
- [5] SepFormer / "Attention is all you need in speech separation" (Subakan et al., ICASSP 2021)
- [7] TF-GridNet (Wang et al., 2023) — 本論文の主要比較対象
- [9] Transformer (Vaswani et al., NeurIPS 2017)
- [19] Conformer (Gulati et al., 2020) — Macaron型構造の着想元
- [21] RoFormer / RoPE (Su et al., 2021)
- [22] GLU Variants Improve Transformer (Shazeer, 2020) — SwiGLU
- [23] RMSNorm (Zhang & Sennrich, NeurIPS 2019)
- [24] Group Normalization (Wu & He, ECCV 2018)
- [29] ESPnet-SE (Li et al., SLT 2021)