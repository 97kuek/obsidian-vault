---
date: 2026-06-14
tags:
  - 機械学習
aliases:
  - PAC学習
  - PAC-Learning
  - Probably Approximately Correct
  - SVMとカーネル法
  - マージン最大化とSVM
  - マージン最大化
  - SVM
  - サポートベクトルマシン
  - カーネルトリック
  - カーネル関数
---
## 学習理論とSVM

PAC学習は汎化を「たぶん、だいたい正しい」と定式化する枠組みであり、SVMはマージン最大化によって複雑さを抑える分類器である。PAC学習、VC次元、SVM、カーネル法を、学習可能性から具体的な分類器へ至る流れとしてまとめる。

## PAC学習（Probably Approximately Correct）

「完璧」を諦め、「**たぶん（Probably）・だいたい（Approximately）・正しい（Correct）**」を現実的な合格ラインにする学習の枠組みである。

入力 $x$ は確率分布 $\mathcal{D}$ に従って現れると考える。真の関数 $h^*$ と候補 $h$ の誤差は、答えが食い違う $x$ の確率の合計で測る。PAC学習可能とは、任意の分布と任意の $\varepsilon, \delta$ に対し、多項式時間のアルゴリズムが

$$\Pr\big[\,d_X(h^*, h) \leq \varepsilon\,\big] \geq 1 - \delta$$

を満たす $h$ を出力できることである。

- **$\varepsilon$（近似度）**：許容する誤差の量である。
- **$\delta$（信頼度）**：失敗してもよい確率であり、$1-\delta$ 以上の確率で成功すればよい。
- 完璧を求める「極限による同定（Gold）」が厳しすぎた反省から、Valiantが1984年に提案した。
- 弱PAC学習を強める考え方はブースティングへ、複数回の試行と多数決はアンサンブルへつながる。
- 必要な訓練データ数と仮説集合の複雑さは、[[次元の呪い|VC次元]]によって関係づけられる。

## マージン最大化（SVM）

分離境界の候補は無数にあるが、**最も近い点までの距離（マージン）が最大になる境界**を選ぶ。マージンを広くとる＝境界をシンプルに保つことであり、実質的に複雑さ（VC次元）を抑えて**過学習を防ぐ**（→ [[次元の呪い]]）。

最も近い点で $|f(\boldsymbol{x})|=1$ になるようスケールを固定するとマージンは $1/|\boldsymbol{w}|$ になり、最大化問題は次の2次計画問題に書き換わる。

$$\min_{\boldsymbol{w}, b}\ \tfrac{1}{2}|\boldsymbol{w}|^2 \quad \text{s.t.}\quad y_k f(\boldsymbol{x}_k) \geq 1$$

境界を実際に決めるのは、マージン上に乗る少数の点＝**サポートベクトル**だけ。

現実のデータはきれいに分離できないので、はみ出しを許す**スラック変数** $\zeta_k$ を導入し、ペナルティ $C\sum_k \zeta_k$ を足す（**ソフトマージン**）。$C$ は「マージンの広さ」と「誤分類の少なさ」のバランスを取るつまみ。

## カーネルトリック

低次元で分けられないデータも、高次元へ写像 $\phi(\boldsymbol{x})$ すれば超平面で分けられることがある。しかし $\phi$ を陽に計算するとコストが跳ね上がる。

ここで SVM の**双対問題が内積の形しか含まない**ことが効く。写像後の内積が、元の空間で計算できる関数 $K$ で書けるなら——

$$\phi(\boldsymbol{x}_j)\cdot\phi(\boldsymbol{x}_k) = K(\boldsymbol{x}_j, \boldsymbol{x}_k)$$

$\phi$ を一切計算せず $K$ だけで高次元での分類ができる。これがカーネルトリック。双対問題の内積 $\boldsymbol{x}_j\cdot\boldsymbol{x}_k$ を $K(\boldsymbol{x}_j, \boldsymbol{x}_k)$ に置き換えるだけで非線形分類になる。$K$ が満たすべき条件が**正定値カーネル**：(1) 対称性 $K(x,y)=K(y,x)$、(2) 任意の点から作るグラム行列が正定値。

## 他の概念との関係

- 関連: [[次元の呪い]]（マージン最大化で複雑さを抑え過学習を防ぐ）
- 対比: [[アンサンブルと集合知]]（弱学習器を束ねて強くする別の発想）
- 関連: [[アンサンブルと集合知]]（弱PAC学習を通常のPAC学習へ押し上げる）

## 出典・根拠

- [[08_PAC学習とSVM|人工知能：PAC学習とSVM]] — PAC学習、VC次元、SVM、カーネル法の章
